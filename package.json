{
    "scripts": {
        "compile": "hardhat compile",
        "deploy_dev": "hardhat run --network dev scripts/deploy_factory.js",
        "deploy_do_nothing_dev": "hardhat run --network dev scripts/deploy_do_nothing.js"
    },
    "dependencies": {
        "@nomicfoundation/hardhat-toolbox": "^2.0.0",
        "dotenv": "^16.0.3",
        "hardhat": "^2.12.1"
    }
}
